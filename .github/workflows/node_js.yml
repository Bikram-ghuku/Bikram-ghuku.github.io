name: Build and Deploy
on: [push]
permissions: 
  contents: write
jobs:
  build-and-deploy: # Recommended if you intend to make multiple deployments in quick succession.
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v3

      - name: Install and Build 🔧 # This example project is built using npm and outputs the result to the 'build' folder. Replace with the commands required to build your project, or remove this step entirely if your site is pre-built.
        run: |
          npm i
          npm run build

      - name: Deploy 🚀
        uses: JamesIves/github-pages-deploy-action@v4.3.3
        with:
          branch: gh-pages 
          folder: build 
      - name: deploy_2
        run: |
          git clone https://github.com/Bikram-ghuku/bikram-ghuku.github.io target
          rm -r ./target/*.html, ./target/*.json, ./target/static
          cp ./build/ ./target/
          cd ./target/
          git add .
          git commit -m "Automatic publish from github.com/bikramghuku-website"
          git push -u -f origin main
